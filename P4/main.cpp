# include <iostream>
# include <stdlib.h>
# include <time.h>
# include "Process.h"

const unsigned RUN_TIME = 60000; // milliseconds

int main() {
    srand(time(NULL));

    std::cout << "running" << std::endl;
    // generate processes
}
